---
title: "Supabase에서 데이터를 읽는데 빈 배열만 와요"
category: 데이터
order: 10
---
에러 없이 `[]`만 온다면 90%는 **RLS**입니다. 테이블에 RLS가 켜져 있는데 정책(Policy)이 없으면 아무것도 안 보입니다.

- 학습 중이면: Table Editor에서 RLS를 잠시 끄고 확인
- 로그인을 붙였다면: "자기 user_id 행만 select" 정책이 있는지, 그리고 지금 **로그인된 상태**인지 확인

나머지 10%는 `.from("테이블이름")`의 오타입니다.
